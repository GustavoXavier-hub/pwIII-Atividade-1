

export let  calc  = function(largura:number,comprimento:number){

    
    return (largura*comprimento)
      
    
    }

export let cubo = (valor:number)=>(valor*valor*valor)

export let funcao = function(nm: number): boolean{
    if(n%2 == 0){
        console.log('par')
        return true
    }
    else{
        console.log('impar')
        return false
    }
}